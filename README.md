# 📞 Call Center Reporting System

> Современная система отчетности для колл-центра с красивым UI и мобильной адаптацией

[![Live Demo](https://img.shields.io/badge/demo-live-success)](https://call.it-mydoc.ru)
[![Node.js](https://img.shields.io/badge/node-%3E%3D18.0.0-brightgreen)](https://nodejs.org/)
[![React](https://img.shields.io/badge/react-18.2.0-blue)](https://reactjs.org/)
[![PostgreSQL](https://img.shields.io/badge/postgresql-14%2B-blue)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

Система отчетности для колл-центра с возможностью ввода данных супервизором и просмотра отчетов руководством.

## 🚀 Возможности

- **Управление операторами**: Добавление, редактирование и удаление операторов
- **Ввод отчетов**: Старшая отдела вводит данные 3 раза в день (утро, день, вечер)
- **Аналитика**: Автоматический расчет KPI и метрик эффективности
- **Отчеты**: Дневные и месячные отчеты с детализацией
- **Роли пользователей**: Supervisor (ввод данных), Management (просмотр отчетов), Admin
- **Мобильная адаптация**: Полностью адаптивный дизайн для всех устройств
- **Современный UI**: Красивый интерфейс с градиентами и иконками

## 📸 Скриншоты

### Дневной отчет
Просмотр статистики по операторам за выбранный день с разбивкой по временным слотам (утро, день, вечер).

### Месячный отчет
Сводная статистика за месяц с KPI: общее количество звонков, записей, средняя длительность, утилизация.

### Ввод данных
Форма для супервизора с возможностью ввода данных по каждому оператору и временному периоду.

## 📊 Метрики

Для каждого временного промежутка отслеживается:
- Общее количество звонков (нарастающим итогом)
- Принятые входящие звонки
- Совершенные исходящие звонки
- Пропущенные звонки
- Время на линии (минуты)
- Время в звонках (минуты)
- Количество записей

Автоматически рассчитывается:
- Средняя длительность звонка
- Коэффициент утилизации (время в звонках / время на линии)
- Среднее количество звонков в день
- Общая статистика по дням и месяцам

## 🛠 Технологический стек

### Backend
- Node.js + Express.js
- PostgreSQL
- JWT Authentication
- bcryptjs для хеширования паролей

### Frontend
- React 18
- React Router v6
- Axios
- TailwindCSS
- Lucide React (иконки)

### Инфраструктура
- Nginx (reverse proxy + static files)
- Let's Encrypt SSL
- systemd (автозапуск)

## 📋 Требования

- Ubuntu 20.04+ (или другой Linux)
- Node.js 20.x
- PostgreSQL 12+
- Nginx
- Доменное имя (для SSL)

## 🔧 Установка и развертывание

### 1. Автоматическая установка (рекомендуется)

```bash
cd /root/callcenter-app
chmod +x deploy.sh
sudo ./deploy.sh
```

Этот скрипт установит все необходимые зависимости:
- Node.js 20.x
- PostgreSQL
- Nginx
- Certbot
- Создаст базу данных
- Установит npm зависимости

### 2. Настройка окружения

```bash
# Скопируйте файл окружения
cp env-production .env

# Отредактируйте .env и измените:
# - JWT_SECRET (обязательно!)
# - DB_PASSWORD (если изменили при установке)
nano .env
```

### 3. Инициализация базы данных

```bash
# Создайте таблицы и начальных пользователей
node server/scripts/init-db.js
```

Будут созданы пользователи:
- `admin` / `admin123` (роль: admin)
- `supervisor` / `supervisor123` (роль: supervisor)
- `manager` / `manager123` (роль: management)

**⚠️ ВАЖНО: Смените пароли после первого входа!**

### 4. Сборка frontend

```bash
cd client
npm run build
cd ..
```

### 5. Настройка Nginx

```bash
chmod +x setup-nginx.sh
sudo ./setup-nginx.sh
```

### 6. Настройка SSL сертификата

```bash
sudo certbot --nginx -d call.it-mydoc.ru
```

Следуйте инструкциям certbot. Сертификат будет автоматически обновляться.

### 7. Установка и запуск сервиса

```bash
chmod +x install-service.sh
sudo ./install-service.sh
```

Приложение будет автоматически запускаться при загрузке сервера.

## 🎯 Использование

### Доступ к приложению

Откройте в браузере: `https://call.it-mydoc.ru`

### Роли и права доступа

**Supervisor (Старшая отдела)**
- Управление списком операторов
- Ввод отчетов 3 раза в день
- Просмотр отчетов

**Management (Руководство)**
- Просмотр дневных отчетов
- Просмотр месячных отчетов
- Анализ KPI операторов
- Доступ к исторической информации

**Admin**
- Все права Supervisor и Management
- Управление пользователями (через БД)

### Рабочий процесс

1. **Утро (08:00-12:00)**: Supervisor вводит данные после 12:00
   - Выбирает оператора
   - Выбирает период "08:00 - 12:00"
   - Вводит накопленные данные (например, всего звонков: 100)

2. **День (12:00-18:00)**: Supervisor вводит данные после 18:00
   - Вводит накопленные данные (например, всего звонков: 250)
   - Система автоматически рассчитает: 250 - 100 = 150 звонков за период

3. **Вечер (18:00-20:00)**: Supervisor вводит данные после 20:00
   - Вводит финальные накопленные данные

4. **Руководство** может в любое время:
   - Выбрать дату и посмотреть дневной отчет
   - Выбрать месяц и посмотреть месячный отчет
   - Сравнить эффективность операторов

## 🔐 Безопасность

- Все пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- HTTPS обязателен в production
- Rate limiting на API
- Helmet.js для безопасности заголовков
- CORS настроен только на ваш домен

## 📱 Управление сервисом

```bash
# Запуск
sudo systemctl start callcenter

# Остановка
sudo systemctl stop callcenter

# Перезапуск
sudo systemctl restart callcenter

# Статус
sudo systemctl status callcenter

# Логи
sudo journalctl -u callcenter -f
```

## 🐛 Отладка

### Проверка API

```bash
# Health check
curl http://localhost:5000/health

# Проверка подключения к БД
sudo -u postgres psql -d callcenter_db -c "SELECT * FROM users;"
```

### Логи

```bash
# Логи приложения
sudo journalctl -u callcenter -n 100

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# Логи PostgreSQL
sudo tail -f /var/log/postgresql/postgresql-*-main.log
```

## 📊 Структура проекта

```
callcenter-app/
├── server/                 # Backend
│   ├── database/
│   │   ├── db.js          # Подключение к БД
│   │   └── schema.sql     # Схема БД
│   ├── middleware/
│   │   └── auth.js        # Аутентификация
│   ├── routes/
│   │   ├── auth.js        # Роуты авторизации
│   │   ├── operators.js   # Роуты операторов
│   │   └── reports.js     # Роуты отчетов
│   ├── scripts/
│   │   └── init-db.js     # Инициализация БД
│   └── index.js           # Главный файл сервера
├── client/                # Frontend
│   ├── public/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Login.js
│   │   │   └── Dashboard.js
│   │   ├── context/
│   │   │   └── AuthContext.js
│   │   ├── services/
│   │   │   └── api.js
│   │   ├── App.js
│   │   └── index.js
│   └── package.json
├── deploy.sh              # Скрипт развертывания
├── setup-nginx.sh         # Настройка Nginx
├── install-service.sh     # Установка systemd сервиса
├── callcenter.service     # Systemd unit file
├── env-production         # Пример .env для production
└── README.md
```

## 🔄 Обновление приложения

```bash
cd /root/callcenter-app

# Остановить сервис
sudo systemctl stop callcenter

# Обновить код (git pull или загрузить новые файлы)

# Обновить зависимости
npm install
cd client && npm install && cd ..

# Пересобрать frontend
cd client && npm run build && cd ..

# Запустить сервис
sudo systemctl start callcenter
```

## 📝 API Endpoints

### Authentication
- `POST /api/auth/login` - Вход
- `POST /api/auth/change-password` - Смена пароля

### Operators
- `GET /api/operators` - Список операторов
- `POST /api/operators` - Создать оператора
- `PUT /api/operators/:id` - Обновить оператора
- `DELETE /api/operators/:id` - Удалить оператора

### Reports
- `GET /api/reports` - Список отчетов (с фильтрами)
- `POST /api/reports` - Создать/обновить отчет
- `DELETE /api/reports/:id` - Удалить отчет
- `GET /api/reports/stats/daily` - Дневная статистика
- `GET /api/reports/stats/monthly` - Месячная статистика

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи сервиса
2. Проверьте логи Nginx
3. Убедитесь, что PostgreSQL запущен
4. Проверьте настройки .env файла
5. Убедитесь, что порт 5000 не занят

## 📄 Лицензия

Proprietary - Все права защищены

## 👥 Автор

Разработано для IT-MyDoc Call Center
