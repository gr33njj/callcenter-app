# ✅ РАЗВЕРТЫВАНИЕ ЗАВЕРШЕНО УСПЕШНО!

## 🎉 Приложение развернуто и работает

**URL приложения:** https://call.it-mydoc.ru

---

## 🔐 Учетные данные для входа

### Supervisor (Старшая отдела)
- **Логин:** `supervisor`
- **Пароль:** `supervisor123`
- **Права:** Управление операторами, ввод отчетов

### Management (Руководство)
- **Логин:** `manager`
- **Пароль:** `manager123`
- **Права:** Просмотр отчетов, анализ KPI

### Admin (Администратор)
- **Логин:** `admin`
- **Пароль:** `admin123`
- **Права:** Полный доступ

⚠️ **ВАЖНО:** Смените пароли после первого входа!

---

## 📊 Что установлено

### Системное ПО
- ✅ Ubuntu 24.04.1 LTS
- ✅ Node.js 20.19.6
- ✅ PostgreSQL 16
- ✅ Nginx 1.24.0
- ✅ Certbot (Let's Encrypt SSL)

### База данных
- ✅ База данных: `callcenter_db`
- ✅ Пользователь: `callcenter_user`
- ✅ Схема создана
- ✅ Тестовые данные загружены

### Приложение
- ✅ Backend API (Express.js) на порту 5000
- ✅ Frontend (React) собран и развернут
- ✅ SSL сертификат установлен (действителен до 2026-03-09)
- ✅ Автозапуск через systemd

---

## 🚀 Управление приложением

### Команды systemd

```bash
# Проверить статус
systemctl status callcenter

# Запустить
systemctl start callcenter

# Остановить
systemctl stop callcenter

# Перезапустить
systemctl restart callcenter

# Посмотреть логи
journalctl -u callcenter -f

# Последние 100 строк логов
journalctl -u callcenter -n 100
```

### Управление Nginx

```bash
# Проверить конфигурацию
nginx -t

# Перезагрузить конфигурацию
systemctl reload nginx

# Перезапустить Nginx
systemctl restart nginx

# Логи Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### База данных PostgreSQL

```bash
# Подключиться к БД
sudo -u postgres psql -d callcenter_db

# Посмотреть пользователей
sudo -u postgres psql -d callcenter_db -c "SELECT * FROM users;"

# Посмотреть операторов
sudo -u postgres psql -d callcenter_db -c "SELECT * FROM operators;"

# Посмотреть отчеты
sudo -u postgres psql -d callcenter_db -c "SELECT * FROM reports;"
```

---

## 📁 Структура файлов на сервере

```
/root/callcenter-app/
├── server/              # Backend API
├── client/              # Frontend (React)
│   └── build/          # Собранное приложение
├── .env                # Конфигурация (НЕ КОММИТИТЬ!)
├── README.md           # Документация
└── *.sh                # Скрипты развертывания
```

---

## 🔒 Безопасность

### Что нужно сделать ОБЯЗАТЕЛЬНО:

1. **Смените пароли пользователей**
   - Войдите под каждым пользователем
   - Используйте функцию смены пароля в приложении

2. **Измените JWT_SECRET**
   ```bash
   nano /root/callcenter-app/.env
   # Измените строку JWT_SECRET на случайную строку
   systemctl restart callcenter
   ```

3. **Настройте firewall (если нужно)**
   ```bash
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw allow 22/tcp
   ufw enable
   ```

### Что уже настроено:

- ✅ SSL/TLS сертификат (HTTPS)
- ✅ Автоматическое обновление SSL (certbot)
- ✅ Rate limiting на API
- ✅ Helmet.js для безопасности заголовков
- ✅ CORS настроен на домен
- ✅ Пароли хешируются с bcrypt
- ✅ JWT токены для аутентификации

---

## 📝 Как использовать приложение

### Для Supervisor (Старшая отдела):

1. Войдите на https://call.it-mydoc.ru
2. Используйте логин `supervisor` / `supervisor123`
3. Добавьте операторов (если нужно)
4. **3 раза в день** вводите отчеты:
   - **После 12:00** - данные за период 08:00-12:00
   - **После 18:00** - данные за период 12:00-18:00
   - **После 20:00** - данные за период 18:00-20:00

### Для Management (Руководство):

1. Войдите на https://call.it-mydoc.ru
2. Используйте логин `manager` / `manager123`
3. Просматривайте отчеты:
   - Выберите дату для дневного отчета
   - Выберите месяц для месячного отчета
4. Анализируйте KPI операторов

---

## 🔄 Обновление приложения

Если нужно обновить код:

```bash
# 1. Остановить сервис
systemctl stop callcenter

# 2. Сделать резервную копию
cp -r /root/callcenter-app /root/callcenter-app.backup

# 3. Обновить файлы (загрузить новые)

# 4. Обновить зависимости (если нужно)
cd /root/callcenter-app
npm install
cd client && npm install && cd ..

# 5. Пересобрать frontend (если изменился)
cd client && npm run build && cd ..

# 6. Запустить сервис
systemctl start callcenter
```

---

## 🆘 Решение проблем

### Приложение не открывается

```bash
# Проверить статус сервиса
systemctl status callcenter

# Проверить логи
journalctl -u callcenter -n 50

# Проверить Nginx
systemctl status nginx
nginx -t
```

### Ошибка подключения к БД

```bash
# Проверить, запущен ли PostgreSQL
systemctl status postgresql

# Проверить подключение
sudo -u postgres psql -d callcenter_db -c "SELECT 1;"

# Проверить права
sudo -u postgres psql -d callcenter_db -c "GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO callcenter_user;"
```

### SSL сертификат истекает

```bash
# Обновить сертификат вручную
certbot renew

# Проверить автообновление
systemctl status certbot.timer
```

---

## 📊 Мониторинг

### Проверка работоспособности

```bash
# API health check
curl https://call.it-mydoc.ru/health

# Проверка API
curl https://call.it-mydoc.ru/api/operators \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Логи в реальном времени

```bash
# Логи приложения
journalctl -u callcenter -f

# Логи Nginx
tail -f /var/log/nginx/access.log

# Логи PostgreSQL
tail -f /var/log/postgresql/postgresql-16-main.log
```

---

## 📞 Контакты и поддержка

**Сервер:** 82.26.93.89  
**Домен:** call.it-mydoc.ru  
**Приложение:** https://call.it-mydoc.ru

---

## ✅ Чек-лист после развертывания

- [ ] Приложение открывается по HTTPS
- [ ] Вход работает для всех ролей
- [ ] Смените пароли по умолчанию
- [ ] Измените JWT_SECRET в .env
- [ ] Проверьте добавление оператора
- [ ] Проверьте ввод отчета
- [ ] Проверьте просмотр отчетов
- [ ] Настройте резервное копирование БД (опционально)

---

**Дата развертывания:** 9 декабря 2025  
**Версия:** 1.0.0  
**Статус:** ✅ Production Ready
